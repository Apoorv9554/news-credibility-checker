# Chrome Extension

This folder will contain the Chrome extension (Manifest V3) that:

- Reads the current page's URL, title, and content
- Sends them to the backend `/api/check` endpoint
- Displays the credibility score and labels in a popup

Implementation will be added in a later phase.
